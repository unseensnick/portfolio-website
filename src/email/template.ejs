<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title><%= headline %></title>
        <% if (preheader) { %>
        <meta name="description" content="<%= preheader %>">
        <% } %>

        <!--[if mso]>
    <noscript>
        <xml>
            <o:OfficeDocumentSettings>
                <o:PixelsPerInch>96</o:PixelsPerInch>
            </o:OfficeDocumentSettings>
        </xml>
    </noscript>
    <![endif]-->

        <style type="text/css">
        /* Reset and base styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body, html {
            margin: 0;
            padding: 0;
            width: 100% !important;
            height: 100% !important;
            font-family: 'Geist', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            -webkit-text-size-adjust: 100%;
            -ms-text-size-adjust: 100%;
            background-color: #f8f9fa;
        }
        
        /* Prevent auto-scaling in iOS */
        .appleLinks a { color: inherit !important; text-decoration: none !important; }
        
        /* Client-specific styles */
        #outlook a { padding: 0; }
        .ReadMsgBody { width: 100%; }
        .ExternalClass { width: 100%; }
        .ExternalClass * { line-height: 100%; }
        
        /* Color scheme - matching project's OKLCH colors */
        :root {
            --primary: #7c3aed; 
            --primary-dark: #6d28d9; 
            --background: #ffffff;
            --foreground: #1a1a1a;
            --muted: #f3f4f6;
            --muted-foreground: #6b7280;
            --border: #e5e7eb;
            --accent: #f8fafc;
        }
        
        /* Typography */
        .headline {
            font-size: 32px;
            font-weight: 700;
            line-height: 1.2;
            color: var(--foreground);
            margin: 0 0 24px 0;
            text-align: center;
        }
        
        .subheadline {
            font-size: 18px;
            font-weight: 500;
            line-height: 1.4;
            color: var(--muted-foreground);
            margin: 0 0 32px 0;
            text-align: center;
        }
        
        .body-text {
            font-size: 16px;
            line-height: 1.6;
            color: var(--foreground);
            margin: 0 0 16px 0;
        }
        
        .small-text {
            font-size: 14px;
            line-height: 1.5;
            color: var(--muted-foreground);
        }
        
        /* Layout containers */
        .email-container {
            max-width: 600px;
            margin: 0 auto;
            background-color: var(--background);
        }
        
        .email-header {
            padding: 40px 40px 0 40px;
            text-align: center;
        }
        
        .email-body {
            padding: 40px;
        }
        
        .email-footer {
            padding: 24px 40px 40px 40px;
            border-top: 1px solid var(--border);
            margin-top: 32px;
        }
        
        /* Logo and branding */
        .logo-container {
            margin-bottom: 32px;
        }
        
        .logo {
            display: inline-flex;
            align-items: center;
            gap: 12px;
            text-decoration: none;
            color: var(--foreground);
        }
        
        .logo-icon {
            width: 40px;
            height: 40px;
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            border-radius: 12px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: 700;
            font-size: 18px;
        }
        
        .logo-text {
            font-size: 20px;
            font-weight: 700;
            color: var(--foreground);
        }
        
        .logo-subtitle {
            font-size: 12px;
            font-weight: 500;
            color: var(--muted-foreground);
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        /* Call-to-action button */
        .cta-container {
            text-align: center;
            margin: 32px 0;
        }
        
        .cta-button {
            display: inline-block;
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            color: white;
            text-decoration: none;
            padding: 16px 32px;
            border-radius: 12px;
            font-weight: 600;
            font-size: 16px;
            line-height: 1;
            transition: all 0.2s ease;
            box-shadow: 0 4px 12px rgba(124, 58, 237, 0.2);
        }
        
        .cta-button:hover {
            background: var(--primary-dark);
            box-shadow: 0 6px 16px rgba(124, 58, 237, 0.3);
            transform: translateY(-1px);
        }
        
        /* Security notice */
        .security-notice {
            background-color: var(--accent);
            border: 1px solid var(--border);
            border-radius: 12px;
            padding: 20px;
            margin: 24px 0;
        }
        
        .security-notice .small-text {
            margin: 0;
        }
        
        /* Responsive design */
        @media only screen and (max-width: 600px) {
            .email-header,
            .email-body,
            .email-footer {
                padding-left: 24px !important;
                padding-right: 24px !important;
            }
            
            .headline {
                font-size: 28px !important;
            }
            
            .subheadline {
                font-size: 16px !important;
            }
            
            .cta-button {
                padding: 14px 24px !important;
                font-size: 15px !important;
                width: auto !important;
                display: inline-block !important;
            }
            
            .logo-icon {
                width: 36px !important;
                height: 36px !important;
                font-size: 16px !important;
            }
            
            .logo-text {
                font-size: 18px !important;
            }
        }
        
        /* Dark mode support for supported clients */
        @media (prefers-color-scheme: dark) {
            body, html {
                background-color: #0f0f0f !important;
            }
            
            .email-container {
                background-color: #1a1a1a !important;
            }
            
            .headline, .body-text, .logo-text {
                color: #f5f5f5 !important;
            }
            
            .security-notice {
                background-color: #262626 !important;
                border-color: #404040 !important;
            }
            
            .email-footer {
                border-color: #404040 !important;
            }
        }
    </style>
    </head>
    <body>
        <!-- Preheader text (hidden but helps with email previews) -->
        <% if (preheader) { %>
        <div
            style="display: none; font-size: 1px; line-height: 1px; max-height: 0; max-width: 0; opacity: 0; overflow: hidden;">
            <%= preheader %>
        </div>
        <% } %>

        <!-- Email container -->
        <div style="background-color: #f8f9fa; padding: 20px 0;">
            <table role="presentation" cellspacing="0" cellpadding="0" border="0" width="100%">
                <tr>
                    <td align="center">
                        <div class="email-container">
                            <!-- Header -->
                            <div class="email-header">
                                <!-- Logo -->
                                <div class="logo-container">
                                    <a href="<%= baseUrl %>" class="logo">
                                        <div class="logo-icon">
                                            &lt;/&gt;
                                        </div>
                                        <div>
                                            <div class="logo-text">unseensnick</div>
                                            <div class="logo-subtitle">Full Stack Developer</div>
                                        </div>
                                    </a>
                                </div>
                            </div>

                            <!-- Body -->
                            <div class="email-body">
                                <!-- Headline -->
                                <h1 class="headline"><%= headline %></h1>

                                <!-- Content -->
                                <div class="body-text">
                                    <%- content %>
                                </div>

                                <!-- Call-to-action button -->
                                <% if (cta) { %>
                                <div class="cta-container">
                                    <a href="<%= cta.url %>" class="cta-button">
                                        <%= cta.buttonLabel %>
                                    </a>
                                </div>
                                <% } %>

                                <!-- Security notice -->
                                <div class="security-notice">
                                    <p class="small-text">
                                        <strong>Security Notice:</strong> If you didn't request a
                                        password reset, please ignore this email.
                                        This link will expire in 1 hour for security reasons. Never
                                        share this email with others.
                                    </p>
                                </div>
                            </div>

                            <!-- Footer -->
                            <div class="email-footer">
                                <p class="small-text"
                                    style="text-align: center; margin: 0 0 8px 0;">
                                    This password reset email was sent by <strong>unseensnick
                                        Portfolio</strong>
                                </p>
                                <p class="small-text" style="text-align: center; margin: 0;">
                                    © <%= currentYear %> All rights reserved.
                                </p>
                            </div>
                        </div>
                    </td>
                </tr>
            </table>
        </div>
    </body>
</html>